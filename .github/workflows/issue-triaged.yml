name: Issue Triaged

on:
  issues:
    types: [labeled]

permissions:
  issues: write

jobs:
  triage:
    name: Move to Backlog
    runs-on: ubuntu-latest
    if: github.event.label.name == 'human'
    env:
      GH_TOKEN: ${{ secrets.GH_PAT }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Remove triage label
        run: |
          gh issue edit ${{ github.event.issue.number }} \
            --remove-label triage \
            --repo ${{ github.repository }} \
            && echo "triage label removed." \
            || echo "triage label was not present — skipping."

      - name: Set Status to Backlog
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          ISSUE_NODE_ID: ${{ github.event.issue.node_id }}
          TARGET_STATUS: Backlog
        run: bash scripts/move-issue-status.sh

      - name: Set Assignee Type to Human
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          ISSUE_NODE_ID: ${{ github.event.issue.node_id }}
        run: |
          source .env.project

          ITEM_ID=$(gh api graphql \
            -H "Authorization: Bearer ${GH_PAT}" \
            -f query='
          {
            node(id: "'"${ISSUE_NODE_ID}"'") {
              ... on Issue {
                projectItems(first: 20) {
                  nodes { id project { id } }
                }
              }
            }
          }' | jq -r --arg pid "$PROJECT_ID" \
              '.data.node.projectItems.nodes[] | select(.project.id == $pid) | .id' \
            | head -1)

          if [[ -z "$ITEM_ID" || "$ITEM_ID" == "null" ]]; then
            echo "ERROR: Issue not found in project ${PROJECT_ID}." >&2
            exit 1
          fi

          gh api graphql \
            -H "Authorization: Bearer ${GH_PAT}" \
            -f query='
          mutation {
            updateProjectV2ItemFieldValue(input: {
              projectId: "'"${PROJECT_ID}"'"
              itemId:    "'"${ITEM_ID}"'"
              fieldId:   "'"${ASSIGNEE_TYPE_FIELD_ID}"'"
              value:     { singleSelectOptionId: "'"${OPT_HUMAN}"'" }
            }) {
              projectV2Item { id }
            }
          }' | jq -e '.data.updateProjectV2ItemFieldValue.projectV2Item.id' > /dev/null
          echo "Assignee Type set to Human."
